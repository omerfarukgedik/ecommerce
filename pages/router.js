import Vue from 'vue'
import VueRouter from 'vue-router'

/*const Home = () => import('~/pages/Home').then(m => m.default || m)
const Categories = () => import('~/pages/Categories').then(m => m.default || m)
const Basket = () => import('~/pages/Basket').then(m => m.default || m)


Vue.use(VueRouter)

export function createRouter() {
  return new VueRouter({
    routes: [
      {
        path: '/',
        name: 'Home',
        component: Home,
      },
      {
        path: '/tr/:slug-arac-kiralama',
        name: 'CarList',
        component: CarList,
        meta: {
          isHeaderShow: true,
          isFooterShow: true
        }
      },
      {
        path: '/login',
        name: 'Login',
        component: Login,
        meta: {
          isHeaderShow: false,
          isFooterShow: false
        }
      },
      {
        path: '/tr/:categorySlug/:slug/',
        name: 'BlogDetail',
        component: BlogDetail
      },
      { path: '*', redirect: '/' },
    ]
  })
}*/